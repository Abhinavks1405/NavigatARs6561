<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/train_image"
        android:label="@string/app_name"
        android:roundIcon="@drawable/train_image"
        android:supportsRtl="true"
        android:theme="@style/Theme.RailwayNavApp"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LanguageScreen.LanguageSelectScreen"/>
        <activity android:name=".NavScreen.NavScreen"/>
        <activity android:name=".OTPScreen.OTPScreen"/>
        <activity android:name=".SignInPage.SignInPage"/>
        <activity android:name=".StationSelectScreen.StationSelectActivity"/>

    </application>

</manifest>